{
  "name": "grubdash-back-end",
  "version": "1.0.0",
  "description": "Backend API for GrubDash food delivery platform",
  "repository": {
    "type": "git",
    "url": "https://github.com/loganprit/grubdash",
    "directory": "backend"
  },
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npm ci --production && npm cache clean --force",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf node_modules"
  },
  "keywords": [
    "express",
    "api",
    "grubdash"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-conf-env": "^1.2.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "supertest": "^5.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
